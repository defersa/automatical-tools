function combobox_ui(options){

	var functions;
	var setting;

	var visual;
	var content;
	
	var link = this;

	link.create = function(options){
		if(!options)			options = {};
 
	}

	link.change = function(options){
		if(options.parent != undefined)		parent.appendChild(visual);
	}

	link.remove = function(){

	}


	link.create(options)
}